=================================
Breadth-first traversal generator
=================================

A breadth-first traversal generator generates events for a
:term:`breadth-first traversal`, which always extends the
shallowest unexplored node first.

Events generated
----------------

Example
-------